{
    {
        {
            {

                {
                    var sera = 'sera??????'
                }
            }
        }
    }
}
console.log(sera)
// a variavel consegue ser lida dentro e fora do bloco, no caso do javascript

function teste() {
    var local = 1500
    console.log(local)
}

/*console.log(local) ==> ao colocar para executar a funcao fora das 
chaves o programa da erro e nao consegue ler e acessar o que esta dentro */
// ou seja, variavel so consegue ser lida fora das chaves se nao for funcao, se for funcao so é lida dentro da funcao.